# spark-defaults.conf
# Worker machine: Kafka + MinIO local → S3A tuning cho local (không qua 4G)

spark.master                     spark://spark-master:7077
spark.app.name                   NYC-Taxi-Pipeline

# ── Delta Lake ───────────────────────────────────────────────
spark.sql.extensions             io.delta.sql.DeltaSparkSessionExtension
spark.sql.catalog.spark_catalog  org.apache.spark.sql.delta.catalog.DeltaCatalog

# ── S3A / MinIO (LOCAL trên Worker machine) ──────────────────
# MinIO local → dùng hostname Docker internal "minio:9000"
spark.hadoop.fs.s3a.endpoint              http://minio:9000
spark.hadoop.fs.s3a.access.key            minioadmin
spark.hadoop.fs.s3a.secret.key            minioadmin
spark.hadoop.fs.s3a.path.style.access     true
spark.hadoop.fs.s3a.impl                  org.apache.hadoop.fs.s3a.S3AFileSystem
spark.hadoop.fs.s3a.connection.ssl.enabled false

# S3A performance — local network, có thể tăng so với qua 4G
spark.hadoop.fs.s3a.connection.maximum    20
spark.hadoop.fs.s3a.threads.max            8
spark.hadoop.fs.s3a.threads.keepalivetime 30
spark.hadoop.fs.s3a.multipart.size        67108864
# disk buffer: tránh áp lực executor heap khi upload
spark.hadoop.fs.s3a.fast.upload.buffer    disk
spark.hadoop.fs.s3a.fast.upload.active.blocks 2
spark.hadoop.fs.s3a.connection.timeout    5000
spark.hadoop.fs.s3a.socket.timeout        15000
spark.hadoop.fs.s3a.attempts.maximum      5

# ── Network — timeout cao cho 4G (Driver↔Executor qua LAN) ──
spark.driver.bindAddress                  0.0.0.0
spark.driver.port                         4100
spark.blockManager.port                   7337
spark.network.timeout                     300s
spark.executor.heartbeatInterval          60s
spark.rpc.askTimeout                      180s
spark.rpc.lookupTimeout                   180s
spark.shuffle.io.maxRetries               10
spark.shuffle.io.retryWait                10s
spark.task.maxFailures                    8
spark.port.maxRetries                     20

# ── Streaming ─────────────────────────────────────────────────
spark.streaming.backpressure.enabled      true
spark.streaming.backpressure.initialRate  500

# ── Shuffle / AQE ─────────────────────────────────────────────
spark.sql.shuffle.partitions              4
spark.sql.adaptive.enabled                true
spark.sql.adaptive.coalescePartitions.enabled true
spark.sql.adaptive.skewJoin.enabled       true
spark.sql.broadcastTimeout                300

# ── Memory ───────────────────────────────────────────────────
spark.memory.fraction                     0.7

# ── Prometheus Metrics Sink ──────────────────────────────────
spark.metrics.conf.*.sink.prometheusServlet.class   org.apache.spark.metrics.sink.PrometheusServlet
spark.metrics.conf.*.sink.prometheusServlet.path    /metrics/prometheus
spark.metrics.conf.master.sink.prometheusServlet.class  org.apache.spark.metrics.sink.PrometheusServlet
spark.metrics.conf.master.sink.prometheusServlet.path   /metrics/master/prometheus/
spark.metrics.conf.worker.sink.prometheusServlet.class  org.apache.spark.metrics.sink.PrometheusServlet
spark.metrics.conf.worker.sink.prometheusServlet.path   /metrics/worker/prometheus/
spark.metrics.conf.driver.sink.prometheusServlet.class  org.apache.spark.metrics.sink.PrometheusServlet
spark.metrics.conf.driver.sink.prometheusServlet.path   /metrics/driver/prometheus/
spark.metrics.conf.*.source.jvm.class     org.apache.spark.metrics.source.JvmSource
spark.sql.streaming.metricsEnabled        true

# ── UI ────────────────────────────────────────────────────────
spark.ui.port                             4040
spark.master.ui.port                      8080
spark.worker.ui.port                      8081